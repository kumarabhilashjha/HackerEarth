#include <stdio.h>
#include <string.h>
int main()
{
    int i,l,flag=1;
    char s[110];
    scanf("%s",s);
    l=strlen(s);
    for(i=0;i<l;i++)
    {if(s[i]==s[l-i-1])
     {flag=1;}
     else{flag=0;}
    }
    if(flag==1){printf("YES");}
    else{printf("NO");}
    return 0;
}
